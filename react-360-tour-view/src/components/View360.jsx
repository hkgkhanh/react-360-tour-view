import React, { useEffect, useRef } from "react";

export default function View360() {
    const panoRef = useRef(null);

    useEffect(() => {
        
    }, []);

    return (
        <></>
    );
}
